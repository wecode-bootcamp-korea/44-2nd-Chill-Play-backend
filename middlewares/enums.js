const seatStatusEnum = Object.freeze({
    VIP: 1,
    REGULAR: 2
  });

  const orderStatusEnum = Object.freeze({
    pending: 1,
    comeplete: 2
  });

  
  module.exports = { seatStatusEnum, orderStatusEnum };
  
  